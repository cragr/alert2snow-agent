apiVersion: v1
kind: Service
metadata:
  name: {{ include "alert2snow-agent.fullname" . }}
  labels:
    {{- include "alert2snow-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "alert2snow-agent.selectorLabels" . | nindent 4 }}
